-title "Sign in"

/ Signing in is temporarily disabled while we update our servers. We hope to enable it soon! 
= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| 
  = f.input :username
  = f.input :password

  = f.submit "Sign in", :class => "btn primary"

